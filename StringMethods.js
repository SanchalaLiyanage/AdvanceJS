//These are built-in functions

let str= "  Navodi Sanchala  ";

console.log(str);
let a=str.toUpperCase(); //Here we need to create another variable to store that valur. because these methods are not changing the original string.
console.log(a);

let b= str.toLowerCase();
console.log(b)


//Never a original string changes. So we need to create a new string everytime for changes
//Strings in java script is immutable

let ab = str.trim(); //Trim is used to remove unessasary spaces in the string
console.log(ab);


