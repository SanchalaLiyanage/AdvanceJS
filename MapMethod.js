//Map Creates a new array with the results of some operation.
//The value its callback returns are used to form new array

//arr.mao(callbackFnx(value,index,array))

let num =[23,34,45,56,67,78,89]

num.map((val) =>{
    console.log(val)
})

let calcSquare = (num) => {
    console.log(num )
}
